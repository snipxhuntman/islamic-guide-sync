import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.lovable.alrahmanmoschee',
  appName: 'islamic-guide-sync',
  webDir: 'dist'
};

export default config;
